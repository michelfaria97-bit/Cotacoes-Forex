name: Manter CotacoesForex Vivo

on:
  schedule:
    # Roda a cada 6 horas (00:00, 06:00, 12:00, 18:00 UTC)
    - cron: '0 */6 * * *'
  workflow_dispatch: # Permite que você teste clicando em um botão

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Acessar App
        run: curl -I https://cotacoesforex.streamlit.app/
